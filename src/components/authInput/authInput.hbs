<div class="auth-input">
  <label class="auth-input__label" for="auth-input__input_type_{{name}}">{{placeholder}}</label>
  <input class="auth-input__input" id="auth-input__input_type_{{name}}"
         type="{{type}}"
         name="{{name}}"
         placeholder="{{placeholder}}"
         minlength="{{minlength}}"
         maxlength="{{maxlength}}"
         {{#if pattern}}pattern="{{pattern}}"{{/if}}
         title="{{errorText}}"
         required
  />
  <span class="auth-input__error">{{errorText}}</span>
</div>
